import { GearIcon } from "@radix-ui/react-icons";

export const SettingsButtonLabel = () => (
	<span className="flex flex-nowrap">
		<GearIcon className="mr-[0.2rem] mt-[0.2rem] inline" />
		Settings
	</span>
);
